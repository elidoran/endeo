# # Endeo's import() method.
# #
# # this adds what was produced by an earlier add(creator).
# # the properties array with all the info setup.
# # this also allows dev's to import this from a JSON file instead of
# # an object to then make into a creator function. cool.
# module.exports = (spec) ->
